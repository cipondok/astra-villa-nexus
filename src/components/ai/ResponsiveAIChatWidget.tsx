
import React from 'react';

const ResponsiveAIChatWidget: React.FC = () => {
  // Simple fallback component to prevent rendering errors
  console.log('ResponsiveAIChatWidget rendered');
  
  return (
    <div className="fixed bottom-4 right-4 z-50">
      {/* Chat widget temporarily disabled for debugging */}
    </div>
  );
};

export default ResponsiveAIChatWidget;
